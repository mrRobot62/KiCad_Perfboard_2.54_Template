# Assets

Images used by the repository README and project documentation belong in
this directory.

Suggested filenames already referenced by placeholders in the main
README:

-   `perfboard-pcb-example.png`
-   `perfboard-schematic-example.png`
-   `perfboard-grid.png`
-   `perfboard-layers.png`
-   `perf-solderpoint.png`
-   `cd4033-example.png`

When an image is available, replace the corresponding `<tbd img ...>`
line in `README.md` with the prepared Markdown image line directly below
it.

Prefer cropped, readable screenshots with consistent KiCad zoom levels.
Avoid publishing personal paths, usernames, or unrelated desktop
content.
